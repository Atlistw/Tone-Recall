// Copy values from this example into src/supabase-config.js.
// The anon/publishable key is safe to use in browser code. Never use a service-role key.
window.TONE_RECALL_SUPABASE_CONFIG = {
  url: "",
  anonKey: "",
  redirectTo: ""
};
